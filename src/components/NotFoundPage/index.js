import React, {Component} from 'react'

// TODO add "back to home page"
class NotFoundPage extends Component{
    render() {
        return(
            <div>
                <h1>Nothing found</h1>
            </div>

        )
    }
}

export default NotFoundPage